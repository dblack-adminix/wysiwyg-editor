# Руководство по публикации NPM пакета

## 📦 Подготовка к публикации

Этот документ описывает шаги для публикации WYSIWYG редактора как npm пакета для легкой интеграции в другие React проекты.

## 🎯 Текущее состояние

### ✅ Что уже готово:
- Headless архитектура (core + components)
- TypeScript с полной типизацией
- Vite конфигурация для library mode
- Экспорты в src/index.ts
- Базовая документация в README.md
- package.json с основными полями
- Shiki подсветка синтаксиса для 48+ языков
- Автоопределение кода при вставке
- Поддержка изображений, видео, таблиц
- Мобильная адаптация
- Темная/светлая тема

### 🔧 Что нужно доработать:
1. Обновить package.json (exports, repository, author)
2. Проверить сборку и TypeScript declarations
3. Дополнить документацию примерами интеграции
4. Создать CHANGELOG.md и LICENSE
5. Протестировать в отдельном проекте
6. Опубликовать в npm

## 📋 Пошаговая инструкция

### Шаг 1: Выбор имени пакета

Два варианта:

**Вариант 1: Scoped пакет (рекомендуется)**
```
@your-username/wysiwyg-editor
@your-org/wysiwyg-editor
```
Преимущества:
- Уникальное имя гарантировано
- Можно группировать пакеты
- Выглядит профессионально

**Вариант 2: Обычный пакет**
```
my-wysiwyg-editor
awesome-wysiwyg-editor
```
Преимущества:
- Короче писать
- Проще запомнить

Проверьте доступность имени:
```bash
npm search @your-org/wysiwyg-editor
```

### Шаг 2: Обновление package.json

Откройте `package.json` и обновите:

```json
{
  "name": "@your-org/wysiwyg-editor",
  "version": "1.0.0",
  "description": "Профессиональный WYSIWYG редактор для React с headless архитектурой и Shiki подсветкой синтаксиса",
  "author": "Ваше Имя <your.email@example.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/wysiwyg-editor.git"
  },
  "bugs": {
    "url": "https://github.com/your-username/wysiwyg-editor/issues"
  },
  "homepage": "https://github.com/your-username/wysiwyg-editor#readme",
  "keywords": [
    "wysiwyg",
    "editor",
    "react",
    "contenteditable",
    "rich-text",
    "markdown",
    "headless",
    "typescript",
    "shiki",
    "syntax-highlighting",
    "code-editor",
    "русский",
    "russian"
  ],
  "main": "./dist/index.js",
  "module": "./dist/index.esm.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.esm.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./style.css": "./dist/style.css"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "sideEffects": [
    "*.css"
  ],
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^4.0.0",
    "shiki": "^3.21.0"
  },
  "devDependencies": {
    "@testing-library/react": "^14.1.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "@vitejs/plugin-react": "^4.2.0",
    "eslint": "^8.54.0",
    "eslint-plugin-react": "^7.33.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "jsdom": "^23.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest",
    "lint": "eslint src --ext ts,tsx",
    "prepublishOnly": "npm run build"
  }
}
```

**Что изменить:**
- `name` → ваше имя пакета
- `author` → ваше имя и email
- `repository.url` → ваш GitHub репозиторий
- `bugs.url` → URL для issues
- `homepage` → URL главной страницы

**Новые поля:**
- `exports` — современный способ экспорта модулей
- `sideEffects` — для tree-shaking (CSS имеет side effects)
- `prepublishOnly` — автоматическая сборка перед публикацией

### Шаг 3: Обновление Vite конфигурации

Откройте `vite.config.ts` и обновите:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { resolve } from 'path';

export default defineConfig({
  plugins: [react()],
  
  // Для разработки (demo)
  root: 'demo',
  server: {
    port: 5176,
    host: '0.0.0.0',
    strictPort: true
  },
  
  // Для сборки библиотеки
  build: {
    outDir: '../dist',
    emptyOutDir: true,
    
    lib: {
      entry: resolve(__dirname, 'src/index.ts'),
      name: 'WysiwygEditor',
      formats: ['es', 'cjs'],
      fileName: (format) => {
        if (format === 'es') return 'index.esm.js';
        if (format === 'cjs') return 'index.js';
        return `index.${format}.js`;
      }
    },
    
    rollupOptions: {
      // Не включать React в bundle
      external: ['react', 'react-dom', 'react/jsx-runtime'],
      output: {
        globals: {
          react: 'React',
          'react-dom': 'ReactDOM',
          'react/jsx-runtime': 'react/jsx-runtime'
        },
        // CSS в отдельный файл
        assetFileNames: (assetInfo) => {
          if (assetInfo.name === 'style.css') return 'style.css';
          return assetInfo.name || 'assets/[name][extname]';
        }
      }
    },
    
    // CSS в один файл
    cssCodeSplit: false,
    
    // Source maps для отладки
    sourcemap: true,
    
    // Минификация
    minify: 'esbuild'
  }
});
```

**Ключевые изменения:**
- `formats: ['es', 'cjs']` — ESM и CommonJS
- `external` — React не включается в bundle
- `cssCodeSplit: false` — CSS в один файл
- `sourcemap: true` — для отладки
- `assetFileNames` — CSS называется `style.css`

### Шаг 4: Обновление tsconfig.json

Создайте отдельный `tsconfig.build.json` для сборки:

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationDir": "./dist",
    "emitDeclarationOnly": true,
    "outDir": "./dist",
    "noEmit": false
  },
  "include": ["src"],
  "exclude": [
    "node_modules",
    "dist",
    "demo",
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/__tests__/**"
  ]
}
```

Обновите `package.json` script:

```json
{
  "scripts": {
    "build": "tsc --project tsconfig.build.json && vite build"
  }
}
```

### Шаг 5: Создание LICENSE

Создайте файл `LICENSE`:

```
MIT License

Copyright (c) 2025 Ваше Имя

Данная лицензия разрешает лицам, получившим копию данного программного 
обеспечения и сопутствующей документации (в дальнейшем именуемыми «Программное 
Обеспечение»), безвозмездно использовать Программное Обеспечение без ограничений, 
включая неограниченное право на использование, копирование, изменение, слияние, 
публикацию, распространение, сублицензирование и/или продажу копий Программного 
Обеспечения, а также лицам, которым предоставляется данное Программное Обеспечение, 
при соблюдении следующих условий:

Указанное выше уведомление об авторском праве и данные условия должны быть включены 
во все копии или значимые части данного Программного Обеспечения.

ДАННОЕ ПРОГРАММНОЕ ОБЕСПЕЧЕНИЕ ПРЕДОСТАВЛЯЕТСЯ «КАК ЕСТЬ», БЕЗ КАКИХ-ЛИБО ГАРАНТИЙ, 
ЯВНО ВЫРАЖЕННЫХ ИЛИ ПОДРАЗУМЕВАЕМЫХ, ВКЛЮЧАЯ ГАРАНТИИ ТОВАРНОЙ ПРИГОДНОСТИ, 
СООТВЕТСТВИЯ ПО ЕГО КОНКРЕТНОМУ НАЗНАЧЕНИЮ И ОТСУТСТВИЯ НАРУШЕНИЙ, НО НЕ 
ОГРАНИЧИВАЯСЬ ИМИ. НИ В КАКОМ СЛУЧАЕ АВТОРЫ ИЛИ ПРАВООБЛАДАТЕЛИ НЕ НЕСУТ 
ОТВЕТСТВЕННОСТИ ПО КАКИМ-ЛИБО ИСКАМ, ЗА УЩЕРБ ИЛИ ПО ИНЫМ ТРЕБОВАНИЯМ, В ТОМ ЧИСЛЕ, 
ПРИ ДЕЙСТВИИ КОНТРАКТА, ДЕЛИКТЕ ИЛИ ИНОЙ СИТУАЦИИ, ВОЗНИКШИМ ИЗ-ЗА ИСПОЛЬЗОВАНИЯ 
ПРОГРАММНОГО ОБЕСПЕЧЕНИЯ ИЛИ ИНЫХ ДЕЙСТВИЙ С ПРОГРАММНЫМ ОБЕСПЕЧЕНИЕМ.
```

### Шаг 6: Создание CHANGELOG.md

Создайте файл `CHANGELOG.md`:

```markdown
# История изменений

Все значимые изменения в этом проекте будут документированы в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [1.0.0] - 2025-01-19

### Добавлено
- Первый релиз
- Headless архитектура с EditorCore
- React компоненты (WysiwygEditor, Toolbar, модальные окна)
- Хук useWysiwygEditor
- Подсветка синтаксиса Shiki для 48+ языков
- Автоматическое определение кода при вставке
- Поддержка загрузки изображений (URL и файл)
- Поддержка видео (YouTube, Vimeo)
- Вставка таблиц
- Поиск и замена
- Печать и экспорт в PDF
- Темная/светлая тема
- Мобильная адаптация
- Автосохранение
- Полная поддержка TypeScript
- Подробная документация на русском и английском

### Возможности
- Форматирование: жирный, курсив, подчеркивание, зачеркивание
- Заголовки (H1-H6), параграфы, цитаты
- Нумерованные и маркированные списки
- Выравнивание текста (слева, по центру, справа, по ширине)
- Цвет текста и фона
- Выбор шрифта и размера
- Вставка ссылок
- Вставка изображений (URL и загрузка)
- Встраивание видео (YouTube, Vimeo)
- Создание таблиц
- Блоки кода с подсветкой синтаксиса
- Выбор эмодзи
- Поиск и замена
- Отмена/повтор
- Полноэкранный режим
- Панель предпросмотра
- Просмотр HTML кода
- Подсчет слов/символов/параграфов
- Экспорт в HTML, текст, PDF
- Копирование HTML в буфер обмена
- Печать

### Поддерживаемые языки для подсветки кода
- Web: HTML, CSS, JavaScript, TypeScript, JSX, TSX, JSON, Markdown
- Backend: Python, Go, PHP, Ruby, Java, C#, C++, C, Rust
- DevOps: YAML, TOML, Dockerfile, Nginx, Terraform, Kubernetes
- Базы данных: SQL, PostgreSQL, MySQL
- Shell: Bash, PowerShell, CMD
- И еще 30+ языков

[1.0.0]: https://github.com/your-username/wysiwyg-editor/releases/tag/v1.0.0
```

### Шаг 7: Сборка пакета

```bash
# Очистить предыдущую сборку
rm -rf dist

# Собрать пакет
npm run build

# Проверить содержимое dist/
dir dist
```

**Ожидаемые файлы в dist/:**
```
dist/
├── index.js          # CommonJS bundle
├── index.js.map      # Source map для CJS
├── index.esm.js      # ES Module bundle
├── index.esm.js.map  # Source map для ESM
├── index.d.ts        # TypeScript declarations
└── style.css         # Bundled CSS
```

**Проверьте размер:**
```bash
# Windows PowerShell
Get-ChildItem dist -Recurse | Measure-Object -Property Length -Sum

# Должно быть примерно 200-400 KB
```

### Шаг 8: Тестирование локально

Создайте тестовый проект:

```bash
# Создать новый React проект
npm create vite@latest test-editor -- --template react-ts
cd test-editor
npm install

# Установить ваш пакет локально
npm install ../wysiwyg-editor
```

Или используйте `npm link`:

```bash
# В папке с редактором
cd wysiwyg-editor
npm link

# В тестовом проекте
cd test-editor
npm link @your-org/wysiwyg-editor
```

Создайте `test-editor/src/App.tsx`:

```tsx
import { WysiwygEditor } from '@your-org/wysiwyg-editor';
import '@your-org/wysiwyg-editor/style.css';
import { useState } from 'react';

function App() {
  const [content, setContent] = useState('<p>Привет, мир!</p>');

  return (
    <div style={{ padding: '20px' }}>
      <h1>Тест редактора</h1>
      <WysiwygEditor
        value={content}
        onChange={(html, meta) => {
          setContent(html);
          console.log('Слов:', meta.wordCount);
        }}
        placeholder="Начните печатать..."
      />
      
      <div style={{ marginTop: '20px' }}>
        <h2>HTML:</h2>
        <pre>{content}</pre>
      </div>
    </div>
  );
}

export default App;
```

Запустите:

```bash
npm run dev
```

**Проверьте:**
- ✅ Редактор отображается корректно
- ✅ Стили применяются
- ✅ TypeScript автодополнение работает
- ✅ Все кнопки работают
- ✅ Вставка кода работает
- ✅ Подсветка синтаксиса работает
- ✅ Нет ошибок в консоли
- ✅ Нет предупреждений TypeScript

### Шаг 9: Проверка перед публикацией

```bash
# Проверить, что будет опубликовано
npm pack --dry-run

# Создать .tgz файл
npm pack

# Проверить размер
dir *.tgz

# Распаковать и проверить содержимое
tar -xzf your-org-wysiwyg-editor-1.0.0.tgz
dir package
```

**Убедитесь:**
- Размер пакета разумный (<500KB)
- Включены только dist/, README.md, LICENSE, CHANGELOG.md
- Не включены src/, demo/, node_modules/, тесты

### Шаг 10: Регистрация в npm

Если у вас еще нет аккаунта:

1. Зайдите на https://www.npmjs.com/signup
2. Создайте аккаунт
3. Подтвердите email

Войдите в npm:

```bash
npm login
```

Введите:
- Username
- Password
- Email
- One-time password (если включена 2FA)

Проверьте:

```bash
npm whoami
```

### Шаг 11: Публикация

```bash
# Для scoped пакета (@your-org/...)
npm publish --access public

# Для обычного пакета
npm publish
```

**Если возникла ошибка:**

```
npm ERR! 402 Payment Required - You must sign up for private packages
```

Добавьте `--access public`:

```bash
npm publish --access public
```

**Успешная публикация:**

```
+ @your-org/wysiwyg-editor@1.0.0
```

### Шаг 12: Проверка публикации

1. Откройте страницу пакета:
   ```
   https://www.npmjs.com/package/@your-org/wysiwyg-editor
   ```

2. Установите из npm:
   ```bash
   npm install @your-org/wysiwyg-editor
   ```

3. Проверьте в новом проекте

### Шаг 13: Git теги и релизы

```bash
# Создать git tag
git tag v1.0.0

# Отправить tag на GitHub
git push origin v1.0.0

# Или все теги
git push --tags
```

Создайте GitHub Release:
1. Зайдите на GitHub → Releases → Create a new release
2. Выберите тег v1.0.0
3. Заголовок: "v1.0.0 - Первый релиз"
4. Описание: скопируйте из CHANGELOG.md
5. Нажмите "Publish release"

### Шаг 14: Обновление README

Добавьте бейджи в начало README.md:

```markdown
# Pro WYSIWYG Editor

![npm version](https://img.shields.io/npm/v/@your-org/wysiwyg-editor)
![npm downloads](https://img.shields.io/npm/dm/@your-org/wysiwyg-editor)
![bundle size](https://img.shields.io/bundlephobia/minzip/@your-org/wysiwyg-editor)
![license](https://img.shields.io/npm/l/@your-org/wysiwyg-editor)
![typescript](https://img.shields.io/badge/TypeScript-Ready-blue)

Профессиональный WYSIWYG редактор для React с headless архитектурой.
```

## 🔄 Обновление пакета

Когда нужно опубликовать новую версию:

```bash
# 1. Внести изменения в код
# 2. Обновить CHANGELOG.md

# 3. Обновить версию
npm version patch  # 1.0.0 -> 1.0.1 (исправления)
npm version minor  # 1.0.0 -> 1.1.0 (новые функции)
npm version major  # 1.0.0 -> 2.0.0 (breaking changes)

# 4. Собрать
npm run build

# 5. Опубликовать
npm publish

# 6. Отправить теги
git push --tags
```

## 📝 Чеклист перед публикацией

### Конфигурация
- [ ] package.json обновлен (name, author, repository, exports)
- [ ] vite.config.ts настроен для library mode
- [ ] tsconfig.build.json создан
- [ ] LICENSE файл создан
- [ ] CHANGELOG.md создан

### Документация
- [ ] README.md дополнен примерами интеграции
- [ ] Добавлены примеры для Next.js, Vite, CRA
- [ ] Документированы все props
- [ ] Добавлены примеры использования хуков
- [ ] Описана функция автоопределения кода

### Сборка и тесты
- [ ] Сборка проходит без ошибок (`npm run build`)
- [ ] Тесты проходят (`npm test`)
- [ ] TypeScript компилируется (`tsc --noEmit`)
- [ ] Размер пакета разумный (<500KB)
- [ ] dist/ содержит все нужные файлы

### Тестирование
- [ ] Пакет протестирован локально (npm link)
- [ ] Протестирован в новом React проекте
- [ ] Все экспорты работают
- [ ] CSS импортируется корректно
- [ ] TypeScript типы доступны и работают
- [ ] Автодополнение работает в IDE

### Git и npm
- [ ] Git репозиторий чистый (нет uncommitted changes)
- [ ] Версия обновлена
- [ ] npm login выполнен
- [ ] npm pack --dry-run проверен

## 🎉 Готово!

Ваш пакет опубликован и доступен для установки:

```bash
npm install @your-org/wysiwyg-editor
```

Использование:

```tsx
import { WysiwygEditor } from '@your-org/wysiwyg-editor';
import '@your-org/wysiwyg-editor/style.css';

function App() {
  return <WysiwygEditor placeholder="Начните печатать..." />;
}
```

## 📊 Мониторинг

После публикации отслеживайте:
- Количество загрузок: https://npm-stat.com/charts.html?package=@your-org/wysiwyg-editor
- Issues на GitHub
- Вопросы пользователей
- Запросы на новые функции

## 🚀 Продвижение

Расскажите о пакете:
- Напишите статью на Medium/Habr
- Поделитесь в Twitter/X
- Добавьте в awesome-react списки
- Создайте демо сайт (Vercel/Netlify)
- Добавьте в Product Hunt

## 📚 Полезные ссылки

- [npm Publishing Guide](https://docs.npmjs.com/packages-and-modules/contributing-packages-to-the-registry)
- [Semantic Versioning](https://semver.org/lang/ru/)
- [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/)
- [Vite Library Mode](https://vitejs.dev/guide/build.html#library-mode)
- [TypeScript Declaration Files](https://www.typescriptlang.org/docs/handbook/declaration-files/introduction.html)
- [npm Badges](https://shields.io/)
