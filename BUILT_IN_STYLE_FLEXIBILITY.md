# 🎨 Гибкость встроенного стиля редактора

## Проблема (была)

Встроенный стиль редактора был жёстко закодирован:
- Тёмный фон (#1e1e2e, #2d2d3f)
- Синие кнопки (#6366f1)
- Фиксированные цвета текста
- Невозможно было изменить без переписания CSS

## Решение (теперь)

Встроенный стиль теперь полностью гибкий благодаря CSS переменным!

### Как это работает

1. **Встроенные значения по умолчанию** в CSS:
```css
:root {
  --primary: #6366f1;
  --bg-primary: #ffffff;
  --text-primary: #212529;
  /* ... и другие переменные */
}
```

2. **Динамическая генерация CSS** при выборе темы:
```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    primary: '#ff6b6b',      // Переопределяет --primary
    bgPrimary: '#fff5f5',    // Переопределяет --bg-primary
  }}
/>
```

3. **Все компоненты используют переменные**:
```css
.toolbarBtn {
  color: var(--text-secondary);
  background: transparent;
}

.toolbarBtn:hover {
  background: rgba(99, 102, 241, 0.2);
  color: var(--primary);  /* Использует переменную! */
}
```

---

## 📋 Все CSS переменные

### Цвета

| Переменная | Назначение | По умолчанию |
|------------|-----------|-------------|
| `--primary` | Основной цвет (кнопки, акценты) | #6366f1 |
| `--primary-dark` | Тёмный оттенок основного | #4f46e5 |
| `--primary-light` | Светлый оттенок основного | #e7f1ff |
| `--bg-primary` | Основной фон | #ffffff |
| `--bg-secondary` | Вторичный фон (toolbar, etc) | #f8f9fa |
| `--text-primary` | Основной текст | #212529 |
| `--text-secondary` | Вторичный текст (labels) | #6c757d |

### Стили

| Переменная | Назначение | По умолчанию |
|------------|-----------|-------------|
| `--border-radius` | Скругление углов | 4px |
| `--border-width` | Толщина границ | 1px |
| `--font-family` | Шрифт | System font stack |
| `--font-size` | Размер шрифта | 14px |

### Тени

| Переменная | Назначение | По умолчанию |
|------------|-----------|-------------|
| `--shadow-sm` | Маленькая тень | 0 1px 2px rgba(0,0,0,0.05) |
| `--shadow-md` | Средняя тень | 0 4px 6px rgba(0,0,0,0.1) |
| `--shadow-lg` | Большая тень | 0 10px 15px rgba(0,0,0,0.1) |

---

## 🎯 Примеры использования

### Пример 1: Изменить основной цвет

**Было (невозможно):**
```tsx
// Нельзя было изменить синий цвет кнопок
<WysiwygEditor />  // Всегда синий
```

**Теперь (легко):**
```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    primary: '#ff6b6b',  // Красный вместо синего!
  }}
/>
```

**Результат:** Все кнопки, акценты и интерактивные элементы станут красными.

### Пример 2: Изменить фон и текст

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    bgPrimary: '#1a1a1a',      // Тёмный фон
    textPrimary: '#ffffff',    // Белый текст
    primary: '#00ff00',        // Зелёные акценты
  }}
/>
```

**Результат:** Редактор с тёмным фоном, белым текстом и зелёными кнопками.

### Пример 3: Изменить шрифт и размер

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    fontFamily: '"Georgia", serif',
    fontSize: '16px',
  }}
/>
```

**Результат:** Редактор с серифным шрифтом и большим размером текста.

### Пример 4: Изменить скругление углов

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    borderRadius: '12px',  // Больше скругления
  }}
/>
```

**Результат:** Все элементы редактора будут иметь скруглённые углы.

### Пример 5: Полная кастомизация

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    // Цвета
    primary: '#ff6b6b',
    primaryDark: '#fa5252',
    primaryLight: '#ffe0e0',
    bgPrimary: '#fff5f5',
    bgSecondary: '#ffe8e8',
    textPrimary: '#2d3748',
    textSecondary: '#718096',
    
    // Стили
    borderRadius: '8px',
    borderWidth: '2px',
    fontFamily: '"Inter", sans-serif',
    fontSize: '15px',
    
    // Тени
    shadowMd: '0 4px 8px rgba(255, 107, 107, 0.15)',
  }}
/>
```

**Результат:** Полностью кастомизированный редактор с красной темой.

---

## 🔄 Как это работает внутри

### 1. Пользователь выбирает тему

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{ primary: '#ff0000' }}
/>
```

### 2. Компонент генерирует CSS

```typescript
const themeConfig = getThemeConfig('custom', customTheme);
const css = generateThemeCSS(themeConfig);
// Результат:
// :root {
//   --primary: #ff0000;
//   --bg-primary: #ffffff;
//   ...
// }
```

### 3. CSS вставляется в DOM

```typescript
const styleElement = document.createElement('style');
styleElement.textContent = css;
document.head.appendChild(styleElement);
```

### 4. Все компоненты используют переменные

```css
.toolbarBtn {
  color: var(--primary);  /* Использует #ff0000 */
}

.editorContent {
  color: var(--text-primary);  /* Использует значение из темы */
}
```

---

## ✅ Что теперь гибко

### Встроенный стиль

- ✅ Основной цвет (primary)
- ✅ Цвета фона (bgPrimary, bgSecondary)
- ✅ Цвета текста (textPrimary, textSecondary)
- ✅ Шрифт и размер
- ✅ Скругление углов
- ✅ Толщина границ
- ✅ Тени

### Компоненты, использующие переменные

- ✅ Toolbar (кнопки, цвета)
- ✅ Editor content (текст, фон)
- ✅ Preview panel (все элементы)
- ✅ Modals (диалоги)
- ✅ Status bar (информация)
- ✅ Find/Replace (поиск)
- ✅ Все остальные элементы

---

## 🧪 Тестирование гибкости

### Тест 1: Изменить цвет кнопок

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{ primary: '#00ff00' }}
/>
```

**Проверьте:** Все кнопки должны быть зелёными.

### Тест 2: Изменить фон

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{ bgPrimary: '#000000' }}
/>
```

**Проверьте:** Фон редактора должен быть чёрным.

### Тест 3: Изменить текст

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{ textPrimary: '#ff0000' }}
/>
```

**Проверьте:** Текст должен быть красным.

### Тест 4: Изменить шрифт

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{ fontFamily: '"Comic Sans MS", cursive' }}
/>
```

**Проверьте:** Текст должен быть в Comic Sans.

### Тест 5: Комбинирование

```tsx
<WysiwygEditor
  themeName="custom"
  customTheme={{
    primary: '#ff6b6b',
    bgPrimary: '#fff5f5',
    textPrimary: '#2d3748',
    borderRadius: '12px',
  }}
/>
```

**Проверьте:** Все изменения должны применяться одновременно.

---

## 📊 Сравнение: До и После

### До (жёсткий стиль)

```tsx
// Невозможно было изменить встроенный стиль
<WysiwygEditor />  // Всегда тёмный с синими кнопками

// Приходилось переписывать CSS
.editorWrapper {
  background: #1e1e2e !important;  /* Жёсткий стиль */
}
```

### После (гибкий стиль)

```tsx
// Легко изменить любой аспект встроенного стиля
<WysiwygEditor
  themeName="custom"
  customTheme={{
    primary: '#ff6b6b',
    bgPrimary: '#fff5f5',
    textPrimary: '#2d3748',
  }}
/>

// Или через CSS переменные
:root {
  --primary: #ff6b6b;
  --bg-primary: #fff5f5;
}
```

---

## 🎉 Преимущества

1. **Полная гибкость** - изменяйте любой аспект встроенного стиля
2. **Без переписания CSS** - используйте встроенные переменные
3. **Консистентность** - все компоненты используют одни переменные
4. **Производительность** - CSS переменные работают быстро
5. **Простота** - просто передайте `customTheme` prop
6. **Масштабируемость** - работает для любого размера проекта

---

## 📚 Дополнительные ресурсы

- [CUSTOMIZATION_GUIDE.md](./CUSTOMIZATION_GUIDE.md) - Полное руководство
- [STYLE_INTEGRATION_GUIDE.md](./STYLE_INTEGRATION_GUIDE.md) - Интеграция стилей
- [QUICK_REFERENCE.md](./QUICK_REFERENCE.md) - Быстрая справка
- [README.md](./README.md) - Основная документация

---

## ✨ Итог

Встроенный стиль редактора теперь полностью гибкий! Вы можете изменить любой аспект внешнего вида, просто передав `customTheme` prop.

**Статус:** ✅ Встроенный стиль полностью гибкий и кастомизируемый
