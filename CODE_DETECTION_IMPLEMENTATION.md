# Реализация автоматического определения кода

## Обзор

Редактор теперь автоматически определяет программный код при вставке и применяет подсветку синтаксиса с цветной рамкой.

## Архитектура

### 1. Утилита определения кода (`src/utils/codeDetector.ts`)

Содержит логику для:
- Определения, является ли текст кодом (`isCode()`)
- Распознавания языка программирования (`detectLanguage()`)
- Оборачивания кода в блок с подсветкой (`wrapCodeBlock()`)

**Поддерживаемые языки:**
- JavaScript
- TypeScript
- Python
- Java
- C#
- PHP
- HTML
- CSS
- SQL
- JSON

### 2. Интеграция в Commands (`src/core/Commands.ts`)

Метод `insertText()` был модифицирован для автоматического определения кода:

```typescript
insertText(text: string): void {
  // Проверяем, является ли текст кодом
  if (this.looksLikeCode(text)) {
    const language = this.detectLanguage(text);
    this.insertHTML(`<pre class="code-block" data-language="${language}"><code class="language-${language}">${this.escapeHtml(text)}</code></pre>`);
  } else {
    document.execCommand('insertText', false, text);
    this.element.focus();
  }
}
```

**Критерии определения кода:**
- Минимум 20 символов
- Минимум 3 характерных признака:
  - Фигурные скобки, квадратные скобки, круглые скобки
  - Ключевые слова (function, const, let, var, class, def, public, private, import, export)
  - Операторы (=, <, >, !)
  - Комментарии (//, /*, */, #, <!--)
  - Многострочный текст

### 3. Подсветка синтаксиса (`src/core/EditorCore.ts`)

Метод `highlightCodeBlocks()` применяет Prism.js для подсветки:

```typescript
private highlightCodeBlocks(): void {
  if (!this.element) return;
  
  const codeBlocks = this.element.querySelectorAll('pre code');
  codeBlocks.forEach((block) => {
    if (window.Prism) {
      window.Prism.highlightElement(block);
    }
  });
}
```

**Триггеры подсветки:**
- При вводе текста (с задержкой 100ms для производительности)
- При вставке контента (с задержкой 150ms)

### 4. Prism.js интеграция (`demo/index.html`)

Подключены CDN ссылки для:
- Основной библиотеки Prism.js
- Темы `prism-tomorrow` (тёмная тема)
- Компонентов для всех поддерживаемых языков

### 5. Стили (`src/WysiwygEditor.module.css`)

Блоки кода оформлены с:
- Цветной рамкой (`border: 1px solid #3d3d5c`)
- Тёмным фоном (`background: #2d2d44`)
- Бейджем с названием языка (через `::before` псевдоэлемент)
- Скруглёнными углами и отступами

```css
.editorContent pre.code-block::before {
  content: attr(data-language);
  position: absolute;
  top: 8px;
  right: 8px;
  background: #6366f1;
  color: white;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 11px;
  text-transform: uppercase;
  font-weight: 600;
}
```

## Как это работает

### Сценарий 1: Вставка кода через Ctrl+V

1. Пользователь копирует код из IDE/браузера
2. Вставляет в редактор (Ctrl+V)
3. Срабатывает событие `paste`
4. Через 150ms вызывается `highlightCodeBlocks()`
5. Prism.js находит все блоки `<pre><code>` и применяет подсветку
6. Код отображается с цветной рамкой и бейджем языка

### Сценарий 2: Программная вставка через insertText()

1. Вызывается `commands.insertText(codeString)`
2. Метод проверяет `looksLikeCode()`
3. Если это код, определяется язык через `detectLanguage()`
4. Код оборачивается в `<pre class="code-block" data-language="..."><code class="language-...">...</code></pre>`
5. Вставляется через `insertHTML()`
6. Prism.js автоматически применяет подсветку

### Сценарий 3: Ручная вставка через кнопку Code

1. Пользователь нажимает кнопку "Code" на панели инструментов
2. Вызывается `commands.insertCodeBlock()`
3. Вставляется базовый блок `<pre><code>код</code></pre>`
4. Пользователь может отредактировать содержимое
5. При вводе срабатывает `handleInput()` → `highlightCodeBlocks()`

## Производительность

### Оптимизации:
- **Debouncing**: Подсветка вызывается с задержкой (100-150ms) для избежания лишних вызовов
- **Селективная обработка**: Обрабатываются только блоки `<pre><code>`, а не весь контент
- **Ленивая загрузка**: Prism.js загружается только один раз через CDN

### Потенциальные улучшения:
- Добавить debounce для множественных вставок
- Кэшировать результаты определения языка
- Использовать Web Workers для больших блоков кода

## Тестирование

### Ручное тестирование:
1. Откройте `http://localhost:5176`
2. Используйте примеры из `CODE_DETECTION_TEST.md`
3. Вставьте код в редактор
4. Проверьте:
   - ✅ Код автоматически обёрнут в блок
   - ✅ Применена подсветка синтаксиса
   - ✅ Отображается бейдж с языком
   - ✅ Цветная рамка присутствует

### Автоматическое тестирование:
```typescript
// Пример unit теста
import { isCode, detectLanguage } from './codeDetector';

test('should detect JavaScript code', () => {
  const code = 'function hello() { console.log("Hi"); }';
  expect(isCode(code)).toBe(true);
  expect(detectLanguage(code)).toBe('javascript');
});
```

## Известные ограничения

1. **Минимальная длина**: Код должен быть минимум 20 символов
2. **Характерные признаки**: Требуется минимум 3 признака кода
3. **Короткие фрагменты**: Могут не определяться автоматически (используйте кнопку Code)
4. **Смешанный контент**: Если вставить текст с кодом, весь блок может быть определён как код

## Будущие улучшения

### Возможные доработки:
- [ ] Добавить больше языков (Ruby, Go, Rust, Kotlin, Swift)
- [ ] Улучшить алгоритм определения для коротких фрагментов
- [ ] Добавить ручной выбор языка через dropdown
- [ ] Поддержка inline кода (не только блоки)
- [ ] Копирование кода в буфер обмена одной кнопкой
- [ ] Нумерация строк
- [ ] Подсветка конкретных строк
- [ ] Темы подсветки (выбор между несколькими темами Prism)

### Интеграция с другими инструментами:
- Monaco Editor для более продвинутой подсветки
- CodeMirror как альтернатива
- Highlight.js вместо Prism.js

## Документация для пользователей

См. раздел "💻 Автоматическое определение кода" в `README.md` для инструкций по использованию.

## Файлы, затронутые изменениями

- ✅ `src/utils/codeDetector.ts` - новый файл с утилитами
- ✅ `src/core/Commands.ts` - модифицирован `insertText()`
- ✅ `src/core/EditorCore.ts` - добавлен `highlightCodeBlocks()` и обработчик paste
- ✅ `demo/index.html` - добавлены CDN ссылки Prism.js
- ✅ `src/WysiwygEditor.module.css` - добавлены стили для блоков кода
- ✅ `README.md` - добавлена документация
- ✅ `CODE_DETECTION_TEST.md` - примеры для тестирования

## Заключение

Функция автоматического определения кода полностью интегрирована в редактор и готова к использованию. Пользователи могут просто вставлять код, и он автоматически получит красивое оформление с подсветкой синтаксиса.
