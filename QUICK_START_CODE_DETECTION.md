# 🚀 Быстрый старт: Автоопределение кода

## Что это?

Редактор автоматически распознаёт программный код при вставке и применяет красивую подсветку синтаксиса с цветной рамкой.

## Как использовать?

### Способ 1: Просто вставьте код (Ctrl+V)

1. Скопируйте любой код из вашей IDE или браузера
2. Вставьте в редактор (Ctrl+V или правый клик → Вставить)
3. **Готово!** Код автоматически оформится

### Способ 2: Кнопка "Code" на панели

1. Нажмите кнопку `</>` на панели инструментов
2. Введите или вставьте код
3. Подсветка применится автоматически

## Пример

### До (обычный текст):
```
function hello(name) {
  console.log(`Hello, ${name}!`);
  return true;
}
```

### После (автоматически):
```
╔══════════════════════════════════════════════╗
║                          [JavaScript] ║
║  function hello(name) {                      ║
║    console.log(`Hello, ${name}!`);          ║
║    return true;                              ║
║  }                                           ║
╚══════════════════════════════════════════════╝
```

С подсветкой синтаксиса:
- `function`, `return` - фиолетовые ключевые слова
- `hello`, `name` - белые идентификаторы
- `"Hello, ${name}!"` - зелёная строка
- `console.log` - жёлтые методы

## Поддерживаемые языки

✅ JavaScript / TypeScript  
✅ Python  
✅ Java  
✅ C#  
✅ PHP  
✅ HTML / CSS  
✅ SQL  
✅ JSON  

## Что вы увидите?

1. **Цветная рамка** вокруг кода (тёмно-синяя)
2. **Бейдж с языком** в правом верхнем углу (синий)
3. **Подсветка синтаксиса** (разные цвета для ключевых слов, строк, комментариев)
4. **Моноширинный шрифт** для лучшей читаемости

## Попробуйте прямо сейчас!

1. Откройте редактор: `http://localhost:5176`
2. Скопируйте этот код:

```javascript
const greet = (name) => {
  console.log(`Hello, ${name}!`);
};

greet("World");
```

3. Вставьте в редактор (Ctrl+V)
4. Наслаждайтесь результатом! 🎉

## Советы

💡 **Для коротких фрагментов** (меньше 20 символов) используйте кнопку "Code"  
💡 **Для inline кода** используйте обратные кавычки: \`код\`  
💡 **Для смены языка** удалите блок и вставьте заново  

## Не работает?

Проверьте:
- ✅ Код содержит минимум 20 символов
- ✅ Код содержит характерные признаки (фигурные скобки, ключевые слова)
- ✅ Prism.js загружен (откройте консоль браузера, проверьте `window.Prism`)

Если всё равно не работает, используйте кнопку "Code" на панели инструментов.

## Больше примеров

См. файл `CODE_DETECTION_TEST.md` для полного набора примеров на всех поддерживаемых языках.
