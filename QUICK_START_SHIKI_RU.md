# Быстрый старт - Подсветка кода Shiki

## Установка

```bash
npm install
```

## Базовое использование

Просто вставьте код в редактор - он будет автоматически определен и подсвечен!

### Пример 1: Вставка PowerShell скрипта

Скопируйте этот код:
```powershell
param([string]$Path = "E:\backup")
Get-ChildItem $Path -Filter *.zip | Remove-Item -Force
```

Вставьте его (Ctrl+V) → Автоматически становится подсвеченным блоком PowerShell кода!

### Пример 2: Вставка Docker Compose

Скопируйте этот код:
```yaml
version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"
```

Вставьте его → Автоматически становится подсвеченным блоком YAML кода!

### Пример 3: Вставка конфига Nginx

Скопируйте этот код:
```nginx
server {
    listen 80;
    server_name example.com;
    location / {
        proxy_pass http://localhost:3000;
    }
}
```

Вставьте его → Автоматически становится подсвеченным блоком Nginx кода!

## Возможности

### 1. Автоматическое определение
- Определяет 48+ языков автоматически
- Порог уверенности: 0.3
- Работает при вставке (Ctrl+V)

### 2. Интерактивные элементы
- **Бейдж языка**: Показывает определенный язык
- **Кнопка копирования** (📋): Копировать код в буфер обмена
- **Кнопка переноса** (↩️): Переключ
ить перенос строк

### 3. Подсветка синтаксиса
- Подсветка качества VS Code
- Использует библиотеку Shiki
- Темная тема (github-dark)

### 4. Поддержка предпросмотра
- Код подсвечивается в панели предпросмотра
- Кнопка копирования работает в preview
- Сохраняет форматирование

## Поддерживаемые языки

**Самые популярные:**
- JavaScript, TypeScript, Python, Go, PHP, Ruby, Java, C#, C++
- HTML, CSS, JSON, YAML, Markdown
- Bash, PowerShell, SQL

**DevOps:**
- Docker, Docker Compose, Kubernetes, Terraform, Ansible
- Nginx, Apache, Caddyfile

**Полный список:** См. `SHIKI_FINAL_RU.md`

## Ручная вставка блока кода

Если автоматическое определение не работает:

1. Нажмите кнопку **Code** на панели инструментов
2. Вставьте ваш код
3. Нажмите на бейдж языка чтобы изменить язык
4. Готово!

## Советы

### Совет 1: Перенос строк
- Нажмите кнопку ↩️ чтобы переключить перенос строк
- По умолчанию: ВКЛ (строки переносятся)
- Полезно для длинных строк

### Совет 2: Копирование кода
- Нажмите кнопку 📋 для копирования
- Работает в редакторе и preview
- Копирует простой текст (без форматирования)

### Совет 3: Смена языка
- Нажмите на бейдж языка (например, "PowerShell")
- Выберите из выпадающего списка
- Код автоматически перекрасится

### Совет 4: Большие файлы кода
- Файлы >10МБ могут вызвать timeout
- Используйте ручную вставку для очень больших файлов
- Рассмотрите разделение на меньшие блоки

## Решение проблем

### Код не определяется?
- Проверьте что уверенность > 0.3
- Используйте кнопку ручной вставки кода
- Проверьте консоль на ошибки

### Код не подсвечивается?
- Подождите инициализации Shiki (~1с первый раз)
- Проверьте консоль браузера на ошибки
- Попробуйте обновить страницу

### Специальные символы?
- Все специальные символы поддерживаются
- Использует Base64 кодирование внутри
- Кавычки, слеши и т.д. работают нормально

## Производительность

- **Первая подсветка**: ~500-1000мс (однократная инициализация)
- **Последующие**: <10мс (кэшировано)
- **Кэш**: макс. 500 записей
- **Процент попаданий**: ~80-90%

## Примеры

### Пример: PowerShell скрипт очистки
```powershell
param(
    [string]$Path = "E:\backup",
    [int]$RetentionCount = 7
)

Get-ChildItem $Path -Filter *.zip | 
    Sort-Object LastWriteTime -Descending | 
    Select-Object -Skip $RetentionCount |
    ForEach-Object { Remove-Item $_.FullName -Force }
```

### Пример: Docker Compose
```yaml
version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html
  app:
    build: .
    environment:
      - NODE_ENV=production
```

### Пример: Конфиг Nginx
```nginx
upstream app {
    server 127.0.0.1:3000;
}

server {
    listen 80;
    server_name example.com;
    
    location / {
        proxy_pass http://app;
        proxy_set_header Host $host;
    }
}
```

## Следующие шаги

- Прочитайте `SHIKI_FINAL_RU.md` для полной документации
- Проверьте `FIXTURES_ALL_LANGUAGES.md` для примеров всех языков
- См. `ADVANCED_CODE_DETECTOR.md` для деталей определения

---

Приятного кодирования! 🚀
